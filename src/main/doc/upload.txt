一，得到服务器的路径
1，request.getSession().getServletContext().getRealPath()；
二，存储图片
1,使用uploadFile插件，将图片储存到服务器中；
2，使用io流将图片存储到服务器中；
三，压缩图片并裁剪图片
四，存到数据库
1，存储相对路径
2，以二进制流存入到数据库
五，展示图片
1，直接在页面取相对路径
2，将以而二进制流存入到数据库的图片用io流读取到页面。